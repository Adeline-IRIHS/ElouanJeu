
				-- SAUVEGARDER DANS LE DATAMANAGER
				if DataManager then
					-- ✅ Récupérer toutes les infos nécessaires
					local brainrotSaveData = {
						Type = brainrot:GetAttribute("Rarity"),
						SlotNumber = platform:GetAttribute("SlotNumber"),
						PriceMultiplier = data.priceMultiplier,
						BasePrice = brainrot:GetAttribute("BasePrice") or 100,
						RizzPerTick = brainrot:GetAttribute("RizzPerTick") or 1
					}
					
					print("💾 SAUVEGARDE BRAINROT:")
					print("   Type:", brainrotSaveData.Type)
					print("   Slot:", brainrotSaveData.SlotNumber)
					print("   Multiplier:", brainrotSaveData.PriceMultiplier)
					
					local addSuccess = DataManager.AddBrainrot(owner, brainrotSaveData)
					
					if addSuccess then
						print("✅ Brainrot ajouté aux données")
						
						-- Forcer une sauvegarde immédiate
						local saveSuccess = DataManager.SaveData(owner)
						
						if saveSuccess then
							print("✅ Sauvegarde réussie!")
						else
							warn("❌ Échec de la sauvegarde!")
						end
					else
						warn("❌ Échec de l'ajout du brainrot!")
					end
				else
					warn("❌❌❌ DATAMANAGER NON DISPONIBLE !")
				end
